import React, { Component } from 'react';

class App extends Component {
  render(state) {
    console.log(state);
    return (
      <div>
        Success
      </div>
    );
  }
}

export default App;
