import React, { Component } from 'react';
import './App.css';

class App extends Component {
  render(state) {
    console.log(state);
    return (
      <div>
        Success
      </div>
    );
  }
}

export default App;
